/* eslint-disable @typescript-eslint/no-unused-vars */
import Address from './address'
import Customer from './customer'

describe('Customer unit tests', () => {
	// it('should throw error when id is empty', () => {
	// 	expect(() => {
	// 		const customer = new Customer('', 'John')

	// 	}).toThrowError('Id is required')
	// })

	// it('should throw error when name is empty', () => {
	// 	expect(() => {
	// 		const customer = new Customer(1, '')

	// 	}).toThrowError('Name is required')
	// })

	// it('should throw error when name is empty', () => {
	// 	const customer = new Customer(1, '')
	// 	customer.changeName('wodson')
	// 	expect(customer.Name).toBe('wodson')
	// })

	// it('should activate customer', () => {
	// 	const customer = new Customer(1, 'stanley')
	// 	const address = new Address('123', 'Street', 'DF', 'City')
	// 	customer.Address = address

	// 	customer.activete()

	// 	expect(customer.isActive()).toBe(true)
	// })

	// it('should deactivate customer', () => {
	// 	const customer = new Customer(1, 'stanley')
	// 	customer.deactivate()

	// 	expect(customer.isActive()).toBe(false)
	// })

	// it('should throw when address is undefined when activate a customer', () => {
	// 	expect(() => {
	// 		const customer = new Customer(1, 'stanley')
	// 		customer.activete()
	// 	}).toThrowError('Address is mandatory to activate a customer')
	// })
})